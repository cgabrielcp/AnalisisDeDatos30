# AnalisisDeDatos30

Git:

Monitoreo del repositorio
- git status

carga a capa staging
- git add .

generar el commit
- git commit -m "Mi primer commit en github"

publicar al repositorio principal
- git push origin main

jalar o actualizar cambios 
- git fetch
- git pull origin main